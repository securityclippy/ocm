# OpenClaw + OCM Docker Compose Configuration
# Run ./scripts/setup.sh to generate this file with proper values

# ===========================================
# Required (auto-generated by setup.sh)
# ===========================================

# OpenClaw Gateway token (for API auth)
# Auto-generated, or set your own: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# OCM master encryption key (64 hex characters)
# Auto-generated, or set your own: openssl rand -hex 32
OCM_MASTER_KEY=

# ===========================================
# Paths (auto-set by setup.sh to ~/.openclaw)
# ===========================================

# Host paths for OpenClaw config and workspace
# These are created by setup.sh with your user's ownership
OPENCLAW_CONFIG_DIR=
OPENCLAW_WORKSPACE_DIR=

# ===========================================
# Ports (optional)
# ===========================================

# OpenClaw Gateway port (default: 18789)
# OPENCLAW_GATEWAY_PORT=18789

# OCM Admin UI port (default: 8080)
# OCM_ADMIN_PORT=8080

# ===========================================
# OpenClaw Image (for docker-compose.openclaw.yml)
# ===========================================

# Build in your OpenClaw repo: docker build -t openclaw:local .
# Default: openclaw:local
# OPENCLAW_IMAGE=openclaw:local
