# OCM + OpenClaw Configuration
#
# Copy this file to .env and fill in the values, or run:
#   ./scripts/setup.sh
#
# ⚠️  SECURITY: Keep this file safe!
#     - Do not commit to version control
#     - Do not share or expose publicly

# ===========================================
# Required: Secrets
# ===========================================

# OCM master encryption key (64 hex characters)
# Encrypts all stored credentials - BACK THIS UP!
# Generate with: openssl rand -hex 32
OCM_MASTER_KEY=

# OpenClaw Gateway authentication token
# Used for API calls between OCM and Gateway
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ===========================================
# Required: Paths
# ===========================================

# Host path to OpenClaw config directory
# Usually ~/.openclaw - setup.sh will auto-detect or create
OPENCLAW_CONFIG_DIR=

# Host path to agent workspace
# Usually ~/.openclaw/workspace
OPENCLAW_WORKSPACE_DIR=

# ===========================================
# Optional: Ports
# ===========================================

# OpenClaw Gateway port (default: 18789)
# OPENCLAW_GATEWAY_PORT=18789

# OCM Admin UI port (default: 8080)
# OCM_ADMIN_PORT=8080

# ===========================================
# Optional: Custom OpenClaw image
# ===========================================

# Docker image for OpenClaw (default: openclaw:local)
# Build with: ./scripts/build-openclaw.sh
# OPENCLAW_IMAGE=openclaw:local
