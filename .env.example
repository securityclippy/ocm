# OpenClaw + OCM Docker Compose Configuration
# Copy this to .env - keys will be auto-generated by ./scripts/setup.sh

# ===========================================
# Required (auto-generated by setup.sh)
# ===========================================

# OpenClaw Gateway token (for API auth)
# Auto-generated, or set your own: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# OCM master encryption key (64 hex characters)
# Auto-generated, or set your own: openssl rand -hex 32
OCM_MASTER_KEY=

# ===========================================
# Paths (optional - defaults to Docker volumes)
# ===========================================

# Host path to OpenClaw config directory
# OPENCLAW_CONFIG_DIR=/path/to/.openclaw

# Host path to workspace
# OPENCLAW_WORKSPACE_DIR=/path/to/workspace

# ===========================================
# Ports (optional)
# ===========================================

# OpenClaw Gateway port (default: 18789)
# OPENCLAW_GATEWAY_PORT=18789

# OCM Admin UI port (default: 8080)
# OCM_ADMIN_PORT=8080

# ===========================================
# Images (optional)
# ===========================================

# OpenClaw image (default: ghcr.io/anthropics/openclaw:latest)
# OPENCLAW_IMAGE=ghcr.io/anthropics/openclaw:latest
